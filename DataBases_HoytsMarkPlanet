-- Crear la base de datos
CREATE DATABASE IF NOT EXISTS moviesDB;
USE moviesDB;

-- Crear la tabla MOVIE
CREATE TABLE IF NOT EXISTS MOVIE (
    id INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(250) NOT NULL,
    director VARCHAR(100),
    anio INT,
    duracion INT,
    genero VARCHAR(50)
);

-- Insertar datos en la tabla MOVIE
INSERT INTO MOVIE (titulo, director, anio, duracion, genero)
VALUES ('Spiderman', 'Sam Raimi', 2002, 121, 'Acción');


INSERT INTO MOVIE (titulo, director, anio, duracion, genero)
VALUES ('John Wick', 'Chad Stahelski', 2014, 101, 'Acción');

INSERT INTO MOVIE (titulo, director, anio, duracion, genero)
VALUES ('El Viaje de Chihiro','Hayao Miyazaki', 2001, 125,'Anime');

INSERT INTO MOVIE (titulo, director, anio, duracion, genero)
VALUES ('Mujercitas', 'Greta Gerwig', 2019, 135, 'Drama');
